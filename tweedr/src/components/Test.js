import React from 'react';
// import TodoItem from './TodoItem';

function Test(props) {

 const renderTodos= function(){
  console.log(props);
   // this return is to return the elements
/*    return (
        this.props.tweed.map((e,i) => {
          // this return is for map
          return (
            <div key={i}>
                <p>tweet: {e.tweed}</p>
            </div>

         )
        })
    )
}*/
   return (

         <div>

          </div>

   )
}

export default Test;